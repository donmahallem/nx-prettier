{
    "executors": {
        "prettier": {
            "implementation": "./dist/prettier",
            "schema": "./cli-schema.json",
            "description": "Runs `echo` (to test executors out)."
        }
    }
}
